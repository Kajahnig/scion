//this is a test config file for the filter_creation_test

//A whitelisting filter with a path to the test topology of the
// whitelisting test
whitelist -path ../whitelisting/test_topology.json -outside ISD

//A path length filter with maximum path length 3
pathLength -max 3

//A per AS rate limiting filter for 5 ASes
asRateLimit -outside 5
